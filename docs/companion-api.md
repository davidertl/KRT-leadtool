# Companion App – Server API Contract

This document describes the server contract that the **Companion App** (in `companion/`) relies on. The backend implements it when `APP_MODULES` includes `voice`. All companion and voice traffic uses the **canonical** `/api/companion/*` and `/api/voice/*` routes only.

## Host layout

| Host | Purpose |
|------|--------|
| **App host** (`DOMAIN`, e.g. `yourdomain.com`) | WebUI, main REST API, Socket.IO, main Discord OAuth callback |
| **Voice host** (`VOICE_DOMAIN`, e.g. `voice.yourdomain.com`) | Companion auth, voice REST, voice WebSocket |

The Companion App must be configured with the **voice host** URL (HTTPS, port 443). It does not use the app host for its API. If verification fails, common causes are: (1) deployment — backend or Nginx not healthy (503), see README "Deployment verification" and "Troubleshooting"; (2) wrong host — user entered the main website URL instead of the voice host.

## Required Discord OAuth redirect URIs

In the Discord application (Developer Portal), add **both** redirect URIs:

1. **WebUI (main app):**  
   `https://<DOMAIN>/api/auth/discord/callback`  
   Example: `https://yourdomain.com/api/auth/discord/callback`

2. **Companion App:**  
   `https://<VOICE_DOMAIN>/api/companion/auth/callback`  
   Example: `https://voice.yourdomain.com/api/companion/auth/callback`

Set `DISCORD_CALLBACK_URL` and `COMPANION_DISCORD_CALLBACK_URL` in `.env` to these values.

## Routes served on the voice host

All URLs below are relative to the voice host base (e.g. `https://voice.yourdomain.com`). Only these canonical paths are served; there are no legacy root paths.

### Unauthenticated (discovery / OAuth)

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/companion/server-status` | Server info. Response: `{ ok: true, data: { version, dsgvoEnabled, debugMode, retentionDays, policyVersion, oauthEnabled } }`. |
| GET | `/api/companion/privacy-policy` | Privacy policy. Response: `{ ok: true, data: { version, text } }`. |
| GET | `/api/companion/auth/discord?state=...` | Starts Discord OAuth; redirects to Discord. |
| GET | `/api/companion/auth/callback` | OAuth callback; exchanges code, creates session, returns HTML success page. |
| GET | `/api/companion/auth/poll?state=...` | Poll for login result. Response: `{ ok: true, data: { status, token?, displayName?, policyVersion?, policyAccepted?, error? } }`. |

### Authenticated (Bearer token)

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/companion/me` | Current user and mission list. |
| GET | `/api/companion/bootstrap?mission_id=...` | Mission details, units, reportable_units. |
| POST | `/api/companion/status` | Submit status message (mission_id, unit_id, message_type, message). If no primary unit, a person is auto-created and set as primary. |
| POST | `/api/companion/units/reset-position` | Reset unit position to origin (body: mission_id, unit_id). Gesamtlead: all; gruppenlead/teamlead: scope. |
| POST | `/api/companion/auth/revoke` | Revoke current companion session. |
| POST | `/api/companion/auth/accept-policy` | Accept privacy policy. |
| POST | `/api/voice/tx-event` | Body: `freqId`, `action` (start/stop), optional `radioSlot`, `metadata`. Response includes `listener_count`. |
| GET | `/api/voice/tx-events` | List TX events (optional query `freqId`, `limit`). |
| POST | `/api/voice/frequencies/join` | Body: `freqId`, optional `radioSlot`. Response: `{ ok: true, listener_count }`. |
| POST | `/api/voice/frequencies/leave` | Body: `freqId`. Response: `{ ok: true, listener_count }`. |
| POST | `/api/voice/frequencies/name` | Body: `freqId`, `name`. No-op; returns `{ ok: true }`. |
| GET | `/api/voice/frequencies/names` | Returns `{ ok: true, data: {} }`. |
| GET | `/api/voice/frequencies` | List frequency membership (query `mission_id`; requires mission member). |

### WebSocket

| Path | Description |
|------|-------------|
| `/voice` | Voice relay (audio frames, join/leave, mute, ping/pong). Requires valid Bearer token in query or header. |

## End-to-end encryption (voice)

Voice audio is **end-to-end encrypted** so the server never has access to plaintext:

- **Scheme:** Per-frequency AES-256-GCM. Each frequency has a 32-byte key generated by the server when the first user joins.
- **Key distribution:** The key is sent to the client in the `join_ok` WebSocket message (`freqKey`, base64). Distribution happens over the same TLS-secured WebSocket; the server never decrypts audio.
- **Frame format:** Binary audio frames use an 8-byte header (freqId + sequence, big-endian) for routing; the payload is either plain Opus or `[12-byte nonce][AES-GCM ciphertext][16-byte tag]`. The relay forwards frames without inspecting or decrypting the payload.
- **Companion:** The app encrypts before send and decrypts after receive when it has a key for the frequency; without a key it falls back to plaintext (and shows a warning). Keys are stored only in memory and cleared on leave/disconnect.

## Implementation references

- **Companion auth, discovery, mission:** [backend/src/routes/companion.js](../backend/src/routes/companion.js)
- **Companion auth helpers:** [backend/src/auth/companion.js](../backend/src/auth/companion.js)
- **Voice REST:** [backend/src/modules/voice/routes.js](../backend/src/modules/voice/routes.js)
- **Voice WebSocket relay:** [backend/src/modules/voice/relay.js](../backend/src/modules/voice/relay.js)

## Operator checklist

- [ ] `VOICE_DOMAIN` and `COMPANION_DISCORD_CALLBACK_URL` set in `.env` (e.g. `VOICE_DOMAIN=voice.yourdomain.com`, `COMPANION_DISCORD_CALLBACK_URL=https://voice.yourdomain.com/api/companion/auth/callback`)
- [ ] Discord app has redirect URI `https://<VOICE_DOMAIN>/api/companion/auth/callback`
- [ ] DNS for `VOICE_DOMAIN` points to the same server as `DOMAIN`; Certbot certificate covers `DOMAIN`, `STATUS_DOMAIN`, and `VOICE_DOMAIN`
- [ ] Nginx (or reverse proxy) serves the voice host and proxies `/api/companion`, `/api/voice`, and `/voice` to the backend
- [ ] `APP_MODULES` includes `voice` so the backend mounts companion and voice routes
- [ ] Post-deploy: backend is healthy, then `curl -sS https://<VOICE_DOMAIN>/api/companion/server-status` returns JSON with `ok: true`. If 503, see README "Deployment verification" and "Troubleshooting"

## Troubleshooting: Voice WebSocket "invalid or expired token"

The Companion connects to `wss://<VOICE_DOMAIN>/voice` and sends a first message `{ type: "auth", authToken: "<jwt>", guildId: "..." }`. The **auth token** is the same JWT the user received after Discord OAuth (from `/api/companion/auth/poll`). The backend relay verifies it with `JWT_SECRET` and requires `id` and `username` in the payload; it does not look up `companion_sessions`.

**When the app shows "Auth failed: invalid or expired token":**

- **JWT verification failed** — `jwt.verify(token, JWT_SECRET)` threw. Common causes:
  1. **Token expired** — Companion JWTs expire after **24 hours**. Have the user **log out and log in again** in the Companion App to get a fresh token.
  2. **JWT_SECRET changed** — After a server redeploy or `.env` change, previously issued tokens are invalid. Users must log in again.
  3. **Wrong or corrupted token** — Stored token from another server or truncated/corrupted in config. Log out and log in again.

**Server-side:** Ensure `JWT_SECRET` is set in `.env` and kept stable across restarts. If you need to debug, check backend logs; the relay can log the verification error (see [backend/src/modules/voice/relay.js](../backend/src/modules/voice/relay.js) `handleAuth`).
